version: '3.8'

services:
  app:
    build: .
    container_name: my_app
    restart: always
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - .:/app
    command: ["npm", "start"]
